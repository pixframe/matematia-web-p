import { stylesheet } from 'typestyle';

const styles = stylesheet({
  container: {
    padding: '2em'
  },
  item: {
    padding: '2em'
  },
  imgStyle: {
    height: '50%'
  }
});

export default styles;
