import { stylesheet } from 'typestyle';

export const styles = stylesheet({
  typeDiv: {
    display: 'flex',
    flexFlow: 'column',
    minHeight: '60vh',
    justifyContent: 'center',
    alignItems: 'center'
  },
  imageIcon: {
    width: '8em',
    height: '8em'
  }
});
